# Toggle Mute Current Program
Simple program to toggle mute of the currently focused program if it matches the executable name set in `config.txt` file.

## Usage instructions
Build the project and add a `config.txt` file next to the `.exe` file. Add a single line to the text file containing only the name of the executable you want to be able to mute.