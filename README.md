# Toggle Mute Current Program
Simple program to toggle mute of the currently focused program if it matches any of the executable names set in `config.txt` file.

## Usage instructions
Build the project and add a `config.txt` file next to the `.exe` file. Add the names of the executables you want to be able to mute all on their own lines (with `.exe` at the end).